schemaVersion: v0.2
prepare:
  steps:
    - name: install
      command: npm install
test:
  steps: []
run:
  service-1:
    steps:
      - name: run
        command: node index.js
    plan: 8
    replicas: 1
    isPublic: true
    network:
      path: /
      stripPath: false
